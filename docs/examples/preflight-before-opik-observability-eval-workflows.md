# Opik observability/eval workflow preflight proof

Customer segment: Opik observability/eval workflow teams using [`comet-ml/opik`](https://github.com/comet-ml/opik) before traced agents, eval datasets, scoring jobs, prompt traces, CI workflows, Docker/deploy files, or secret-adjacent provider keys enter an AI-agent or eval run.

Public target evidence captured during this deadline pulse:

- GitHub target: `comet-ml/opik`
- Stars observed: `20378`
- Archived: `false`
- Last public update: `2026-07-07T01:14:51Z`
- Description: Debug, evaluate, and monitor your LLM applications, RAG systems, and agentic workflows with comprehensive tracing, automated evaluations, and production-ready dashboards.

This is not an endorsement by `comet-ml/opik`. It is a named buyer proof for teams already debugging, evaluating, and monitoring agentic workflows and deciding whether a cheap preflight bundle is worth buying before an agent touches real repo scope.

## 60-second preflight receipt for Opik observability/eval workflow teams

Run the free scanner before a coding agent, eval runner, or observability workflow receives write access, shell access, deployment config, or provider-token-adjacent files:

1. Scan the repo with `python3 agent_preflight_lite.py --repo <repo> --format markdown`.
2. Attach the receipt to the eval run, trace review, pull request, or agent handoff ticket.
3. Mark every package script, workflow file, Docker/deploy file, webhook, MCP/API connector, and secret-adjacent provider setting that the agent can see.
4. Keep the run Green only when no risky scope is present.
5. If the receipt is Yellow/Red, buy the `https://payhip.com/b/1nmxV` pack for the reusable checklist, review-comment template, destructive-command hook starter, and handoff receipt template.

## Copy-paste handoff note

```text
Opik observability/eval workflow teams preflight: ran the free AI Agent Safety lite scanner before widening agent/eval scope.
Target repo or workflow: <repo / eval job / trace run>
Risk level: <Green / Yellow / Red>
Flagged scope: <package scripts, workflows, Docker/deploy files, API tools, provider keys, webhooks, agent instructions>
Allowed next action: <read-only / reviewed shell commands only / stop until owner approves>
Paid-pack trigger: buy the $7 pack only if this receipt is Yellow/Red or the team needs the repeatable hook + review-comment workflow today.
```

## Buy / skip trigger

- **Green:** keep using the free scanner and skip the paid pack for now.
- **Yellow:** buy the $7 pack if the workflow includes package scripts, eval automation, deployment files, MCP/API tools, or provider-token-adjacent config.
- **Red:** stop the agent/eval run and use the paid handoff checklist before any shell, deploy, migration, webhook, or credential-adjacent action continues.

Proof marker: `OPIK_OBSERVABILITY_EVAL_WORKFLOW_PROOF`

Paid pack: https://payhip.com/b/1nmxV
