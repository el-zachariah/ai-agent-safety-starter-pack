# Mirascope tool-calling agent workflow preflight proof

Named buyer segment: **Mirascope tool-calling agent workflow teams** using [`Mirascope/mirascope`](https://github.com/Mirascope/mirascope) or similar workflows before repo context, tool calls, generated code, package scripts, deployment config, or secret-adjacent values enter an AI-assisted run.

Public target evidence captured 2026-07-06T20:06:00-05:00: `Mirascope/mirascope` is public, not archived, observed stars `1513`, updated `2026-07-05T21:03:33Z`, description: "The LLM Anti-Framework".

Marker: `MIRASCOPE_TOOL_CALLING_AGENT_WORKFLOW_PROOF`

## Why this is a checkout-start proof

This is not generic funnel polish. A Mirascope tool-calling agent workflow teams buyer is already near the risky moment: prompt modules, tool calls, response models, eval scripts, package scripts, workflow files, API connectors, and secret-adjacent provider configuration are about to move from prototype scope into a real repository or product workflow. The free scanner gives a visible pre-run receipt first; the paid $7 bundle is only positioned when the receipt is Yellow/Red and the team wants reusable handoff templates, destructive-command guardrails, and reviewer prompts.

## 60-second preflight before the run

```bash
python3 agent_preflight_lite.py /path/to/repo --json > mirascope-tool-calling-agent-workflows-preflight.json
python3 agent_preflight_lite.py /path/to/repo > mirascope-tool-calling-agent-workflows-preflight.md
```

Attach the Markdown receipt to the issue, PR, runbook, or release checklist before widening the Mirascope tool-calling agent run from read-only design/review into write, shell, tool/API, browser, deployment, or credential-adjacent scope.

## Green / Yellow / Red buy trigger

- **Green:** no agent instructions, workflow files, package scripts, MCP/tool config, risky shell patterns, or secret-adjacent files were found. Keep using the free scan and skip the paid pack.
- **Yellow:** package scripts, workflow files, generated commands, tool/API connectors, config files, or secret-adjacent paths need review before the agent/tool workflow runs. Buy the $7 pack if the team wants the reusable approval receipt and reviewer prompts.
- **Red:** destructive commands, broad credential scope, deployment/admin actions, unknown shell pipelines, or risky workflow permissions are present. Stop the run until the scope is reduced; use the paid pack's handoff template and command guardrail before retrying.

## Copy-paste receipt for this segment

```text
Agent Safety Preflight — Mirascope tool-calling agent workflow teams
Target workflow: Mirascope tool-calling agent run
Target repo family: Mirascope/mirascope
Free scan artifact: mirascope-tool-calling-agent-workflows-preflight.md
Decision: Green / Yellow / Red
Allowed next action: <read-only / edit files / run tests / package scripts / deploy>
Must ask before: secrets, auth/session scope, destructive shell, production deploys, payment/admin surfaces, or force-push style git operations
Upgrade trigger: buy https://payhip.com/b/1nmxV only when Yellow/Red means the team needs reusable handoff receipts, destructive-command hooks, and reviewer prompts.
```

No affiliation with `Mirascope/mirascope` is implied; this is a buyer-specific proof showing how the public free scanner and paid starter pack map to a real public workflow.
