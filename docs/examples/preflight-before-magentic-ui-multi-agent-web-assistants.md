# Preflight before Magentic-UI multi-agent web assistant runs

Customer segment: **Magentic-UI multi-agent web assistant teams**.

Public target verified before this proof: [`microsoft/magentic-ui`](https://github.com/microsoft/magentic-ui) is active, not archived, and was observed with **9940** stars, updated `2026-07-06T12:00:56Z`. Description: MagenticLite is an experimental agent that works across the browser and local file system

## Why this proof exists

Magentic-UI multi-agent web assistant buyers are close to the first-dollar pain this product solves: teams running Magentic-UI browser/file/code agents before web tasks, generated code, tool calls, or repo context touch a real project. This page makes the seller/product feel real for that exact segment by showing the free receipt they can run before trusting an agent with repo-changing scope.

## Copy/paste preflight receipt for microsoft/magentic-ui users

Run the free scanner from a copy of the project before widening agent scope:

```bash
python3 agent_preflight_lite.py --path . --profile agent-workflow > agent-preflight-magentic-ui-multi-agent-web-assistant.md
```

Suggested fixture to scan: **Magentic-UI task config plus browser/tool settings, app code paths, .env placeholders, and output directories**.

Paste this receipt into the task issue, PR, or runbook before the agent starts:

```text
Segment: Magentic-UI multi-agent web assistant
Target repo evidence: microsoft/magentic-ui (9940 stars, updated 2026-07-06T12:00:56Z)
Preflight artifact: agent-preflight-magentic-ui-multi-agent-web-assistant.md
Approval gate: human owner reviews Yellow/Red findings before repo-changing commands run
Rollback note: record branch/commit, generated files, and revert command before the agent receives write access
```

## Yellow/Red buy trigger

- **Green:** do not buy yet; keep the free scanner/checklist in the workflow.
- **Yellow:** buy the $7 pack when the receipt finds unclear tool boundaries, missing approval gates, no rollback note, or secret-adjacent config in scope.
- **Red:** buy before the next run when production credentials, destructive commands, deploy files, or workflow automation are in the agent path.

Paid starter pack: https://payhip.com/b/1nmxV

This is a trust proof, not a public endorsement by `microsoft/magentic-ui`.
