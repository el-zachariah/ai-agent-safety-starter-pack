# Preflight before Agent Service Toolkit production agent services

Unique marker: `AGENT_SERVICE_TOOLKIT_PRODUCTION_AGENT_PROOF`

This customer proof is for teams evaluating or using [`JoshuaC215/agent-service-toolkit`](https://github.com/JoshuaC215/agent-service-toolkit) before LangGraph agent services, FastAPI endpoints, Streamlit UI, Docker/deploy config, package scripts, workflow files, API keys, and secret-adjacent runtime settings enter an AI-agent run.

Target verification captured for this proof:

- Repository: `JoshuaC215/agent-service-toolkit`
- Public URL: https://github.com/JoshuaC215/agent-service-toolkit
- GitHub description: Full toolkit for running an AI agent service built with LangGraph, FastAPI and Streamlit
- Stars observed: `4367`
- Updated at: `2026-07-06T15:36:35Z`
- Archived: `False`

## Why this matters before checkout

Production-style agent service buyers do not need another generic prompt sheet; they need proof the seller understands hosted agents, deploy paths, tool authority, logs, and secret boundaries. This page maps the free scanner and paid starter pack to one real public target before payment.

## 60-second buyer check

Run the free scanner before the agent service touches a real repo or deployed environment:

```bash
python3 scripts/agent_preflight_lite.py /path/to/repo
```

Review these items before granting more autonomy:

- Hosted agent services can turn a local demo agent into API-facing behavior, so route/tool scope needs to be frozen before edits.
- FastAPI, Streamlit, Docker, and package scripts can execute after agent-generated changes.
- LangGraph state, tools, and memory config can persist broader authority than one chat session implies.
- CI/deploy workflows can publish agent changes beyond the local workspace.
- Provider keys, env files, webhooks, and service URLs need explicit no-copy/no-log handling before agent runs.

## Paid bundle trigger

Green: use the free scanner only when the run is local, disposable, and read-only.

Yellow: Buy the paid bundle when an agent service is moving from demo to hosted/API-facing code and the team needs a copy-paste preflight receipt before agent edits, deploy scripts, or provider keys enter scope.

Red: do not buy or run the workflow until secrets, payment/KYC surfaces, deploy keys, external-message tools, and irreversible shell commands are removed from scope.

Checkout link for the paid starter pack: https://payhip.com/b/1nmxV

## Copy-paste preflight receipt

```text
Agent Service Toolkit production LangGraph/FastAPI agents preflight receipt
Target repo checked: JoshuaC215/agent-service-toolkit
Scope frozen before run: repo paths, tools, API endpoints, deploy scripts, workflows, logs, and secrets policy
Secrets policy: no private code, tokens, .env values, cookies, customer data, or payment/KYC data copied into agent prompts or receipts
Run decision: Green / Yellow / Red
Owner: Wealth Hunter / Signal Loom Works
```
