# Langroid multi-agent application workflow preflight proof

Unique marker: `LANGROID_MULTI_AGENT_APPLICATION_WORKFLOW_PROOF`

Buyer segment: Langroid multi-agent application teams using [`langroid/langroid`](https://github.com/langroid/langroid) or a similar local/agent workflow before multi-agent task loops, tool calls, repo edits, shell commands, workflow files, vector/db config, and secret-adjacent paths enter scope.

Target evidence captured 2026-07-07T04:25:49.361550-05:00: `langroid/langroid` is public, not archived, observed stars `4055`, updated `2026-07-07T02:43:43Z`, description: "Harness LLMs with Multi-Agent Programming".

## Why this matters before the first Langroid multi-agent application run

Langroid multi-agent application teams are close to the first-dollar buyer because they already let an agent operate near real repositories. Checkout friction drops when the page proves exactly how the public scanner fits their workflow: run the free preflight first, then buy only if the scan is Yellow or Red.

## One-command public preflight

```bash
python3 scripts/agent_preflight_lite.py /path/to/repo --markdown > langroid-multi-agent-application-workflows-preflight-receipt.md
```

## Green / Yellow / Red buy trigger

- **Green:** attach the free receipt to the agent task and skip the paid pack for now.
- **Yellow:** inspect package scripts, workflow files, tool/agent permission surfaces, MCP/tool config, and secret-adjacent paths before widening agent scope.
- **Red:** stop the run; buy the $7 AI Agent Safety Starter Pack (https://payhip.com/b/1nmxV) if you need the full hooks, reviewer prompts, and repeatable handoff receipts before allowing the agent workflow to edit or execute.

## Copy-paste maintainer receipt

```md
Langroid multi-agent application teams preflight receipt
- Target workflow: Langroid multi-agent application run
- Repo scanned before agent authority: <repo/path>
- Decision: Green / Yellow / Red
- Top findings: <3 bullets from agent_preflight_lite.py>
- Scope allowed after review: <files only / tests only / no package scripts / no deploy / no secrets>
- Next gate: rerun before applying changes or enabling shell/package/deploy commands.
```

## What the paid bundle adds for Yellow/Red scans

The public scanner proves fit without requiring a purchase. The $7 pack is for teams that hit Yellow/Red and want ready-to-drop command hooks, reviewer checklists, and handoff receipts so every Langroid multi-agent application run starts with the same safety gate.
