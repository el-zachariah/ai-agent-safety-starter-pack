    # Mastra TypeScript agent framework preflight proof

    Target buyer segment: teams building TypeScript agents, workflows, RAG, tool calls, evals, and deployable agent services with Mastra.

    Public target evidence checked during this pulse: [`mastra-ai/mastra`](https://github.com/mastra-ai/mastra) is public and not archived, with 25866 GitHub stars and latest update timestamp `2026-07-06T15:45:39Z`. Description observed: 'Mastra is the modern TypeScript framework for AI-powered applications and agents.'.

    ## Why this is a checkout-start trust proof

    A Mastra TypeScript agent framework workflow can look like ordinary TypeScript/product code until an agent receives tool, workflow, API, or deploy scope. At that point the repo may expose workflow files, tool/API connectors, RAG/vector settings, package scripts, deployment env, and secret-adjacent config. This page gives that exact buyer a public, copy-paste receipt before buying anything, so the paid bundle feels like a concrete workflow upgrade rather than a cold checkout page.

    ## 60-second free scan

    ```bash
    python3 agent_preflight_lite.py /path/to/repo
    python3 agent_preflight_lite.py /path/to/repo --json
    ```

    Look for these before the agent framework gets real action scope:

    - workflow definitions or agent memory/RAG settings,
- tool/API connector config and MCP-style adapters,
- package scripts, deploy commands, or CI workflows,
- .env, provider keys, datastore URLs, auth/session config, or other secret-adjacent paths,
    - any production or customer-data action that needs a human approval line.

    ## Copy-paste handoff receipt

    ```text
    MASTRA_TYPESCRIPT_AGENT_FRAMEWORK_PROOF
    Target: mastra-ai/mastra
    Free scan result: Green / Yellow / Red
    Agent scope requested: <repo paths, workflows, tools, APIs, package scripts, CI, deployment, data connectors>
    Allowed without asking: <read-only files, safe tests, docs, local scan commands>
    Must ask before: <package install/deploy, credentialed APIs, database writes, auth/session changes, destructive shell, production webhooks>
    Yellow/Red reason: <2+ risk buckets, secret-adjacent config, package scripts, workflow edits, tool/API scope>
    Decision: if Yellow/Red, use the paid starter-pack templates + destructive-command hook before granting tool access; if Green, keep using the free scanner and normal review.
    Paid workflow link if Yellow/Red: https://payhip.com/b/1nmxV
    ```

    ## Buy / skip trigger for this segment

    - **Buy the $7 pack** when the scan is Yellow/Red because workflow files, tool/API connectors, RAG/vector settings, package scripts, deployment env, and secret-adjacent config are in scope and the team needs a repeatable handoff, report template, and destructive-command guardrail before the agent acts.
    - **Skip buying for now** when the scan is Green and the agent has no write-capable tools, no secret-adjacent config, no risky scripts, and no production-facing actions.

    Mastra TypeScript agent framework teams should run the free scan before granting workflow/tool scope, then buy the $7 pack only when the receipt is Yellow/Red.
