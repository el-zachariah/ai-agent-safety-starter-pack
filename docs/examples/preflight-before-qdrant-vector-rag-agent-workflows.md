# Qdrant vector/RAG agent workflow preflight proof

**Named buyer segment:** Qdrant vector-memory and RAG agent workflow teams using [`qdrant/qdrant`](https://github.com/qdrant/qdrant) or similar vector-memory/RAG stacks before vector-memory/RAG agents, collection writes, embedding pipelines, API keys, package scripts, Docker/deploy files, webhooks, or repo context enter an AI-assisted run.

**Public target evidence captured 2026-07-06T17:02:34-05:00:** `qdrant/qdrant` is public and not archived, with 32,984 GitHub stars observed, updated `2026-07-06T21:31:18Z`, and description: "Qdrant - High-performance, massive-scale Vector Database and Vector Search Engine for the next generation of AI. Also available in the cloud https://cloud.qdrant.io/".

## Why this matters before an agent run

Vector databases make agent workflows feel safe because the app is "just retrieval" until the moment an AI task can change collections, write embeddings, call tool endpoints, alter Docker/deploy config, or run package scripts around a real dataset. A first buyer needs proof that the seller understands that edge: the free scanner produces a local receipt before repo/tool scope expands, and the paid pack is positioned only when the receipt is Yellow/Red and the team needs repeatable hooks, reviewer prompts, and handoff templates.

## 60-second preflight receipt

Run the free scanner before widening a Qdrant vector/RAG agent workflow from demo retrieval to write-capable repo, API, deployment, or secret-adjacent scope:

```bash
python3 agent_preflight_lite.py /path/to/repo --markdown > qdrant-vector-rag-agent-workflows-receipt.md
```

Paste the receipt into the PR, issue, runbook, or launch checklist that approves the agent/RAG workflow.

## Buy / skip trigger

- **Green:** keep the free scanner receipt and do not buy yet.
- **Yellow:** buy the $7 pack if package scripts, Docker/deploy files, vector-store config, MCP/API connectors, generated commands, or secret-adjacent paths are in scope and the team needs a reusable review gate.
- **Red:** pause the agent run, reduce scope, and use the paid checklist/templates before the workflow can write collections, run commands, deploy, or touch credentials.

Paid pack: https://payhip.com/b/1nmxV

## Copy-paste approval note

```text
Qdrant vector/RAG agent workflow preflight receipt
Target: qdrant/qdrant
Decision: <Green|Yellow|Red>
Scope requested: <repo files / vector collection writes / tool calls / package scripts / deployment config / secrets>
Action: if Yellow/Red, require the AI Agent Safety Starter Pack checklist plus a review-comment receipt before granting full agent scope.
```

Marker: `QDRANT_VECTOR_RAG_AGENT_WORKFLOW_PROOF`
